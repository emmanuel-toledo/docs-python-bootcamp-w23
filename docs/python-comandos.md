# Comandos en Python

```
# Obtener versión instalada de Python
py --version

# Ejecutar un archivo Python localmente
py ./{nombre archivo}

# Consultar versión de pip
python -m pip --version

# Instalar paquetes con pip
python -m pip install {paquete}
```